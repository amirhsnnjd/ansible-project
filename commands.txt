
1. 
   ansible-inventory -i inventory.ini --list

2. 
   ansible -i inventory.ini all -m ping

3. 
   ansible-playbook -i inventory.ini playbook.yaml

4. 
   ansible -i inventory.ini webservers -m service -a "name=nginx state=started" -b
